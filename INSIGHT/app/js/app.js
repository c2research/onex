var ReactDOM = require('react-dom');
var React = require('react');

var InsightPlatform = require('./components/InsightPlatform');
ReactDOM.render(<InsightPlatform />, document.getElementById('app'));
